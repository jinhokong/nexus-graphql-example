{
  "name": "nexus_example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf ./src/generated && mkdir ./src/generated",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "ts-node -r tsconfig-paths/register ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/uuid": "^3.4.4",
    "ts-node": "^8.0.2",
    "tsconfig-paths": "^3.8.0"
  },
  "dependencies": {
    "apollo-server": "^2.4.0",
    "graphql": "^14.1.1",
    "nexus": "^0.9.9",
    "typescript": "^3.3.3",
    "uuid": "^3.3.2"
  }
}
